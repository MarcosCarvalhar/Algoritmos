#include <stdio.h>
#include <stdlib.h>

int main()
{
    int q=0, vetor[10], c, a, j;
    printf("Digite o valor:");
    scanf("%d", &vetor[0]);
    a=vetor[0];
    for(c=1;c<10;c++){
        printf("Digite o valor:");
        scanf("%d", &vetor[c]);
        for(j=c)
    }

}
